%p
  Note: Servers added here must actually be valid. They are added into the database, and then added into the server setup process as a new server.
  Servers may also be added automatically by using the RESTful server add application. Server IP addresses must be UNIQUE or they will be rejected.
= render 'form'
%nav.back_link
  = link_to 'Back to Servers Overview', test_servers_path
